name: TotemGuard
description: ${description}
authors:
  - Bram
  - OutDev
main: com.deathmotion.totemguard.loader.bukkit.TGLoaderBukkit
version: ${version}
api-version: '1.13'
folia-supported: true
load: POSTWORLD
depend:
  - packetevents
softdepend:
  - PlaceholderAPI
  - GrimAC
commands:
  tgloader:
    description: TotemGuard loader administration (console only).
    usage: /tgloader <restart|status>
